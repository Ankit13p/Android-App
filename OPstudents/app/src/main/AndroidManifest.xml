<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.praja1.opzone.devlopment.android.dell.opstudents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/oplogovectorsquare"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".student_support"></activity>
        <activity android:name=".view_profile" />
        <activity android:name=".edit_profile" />
        <activity android:name=".user_detail" />
        <activity android:name=".register" />
        <activity android:name=".signup_signin" />
        <activity android:name=".contact_us" />
        <activity android:name=".know_about_op" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>